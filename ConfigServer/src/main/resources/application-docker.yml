server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HBirdman/UserPlatformConfig
          default-label: main

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false